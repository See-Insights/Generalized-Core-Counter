#include "Version.h"

// Bump this string and the release notes on each new release.
// Keep the value in sync with the Doxyfile PROJECT_NUMBER.

const char* FIRMWARE_VERSION = "7.00";
const char* FIRMWARE_RELEASE_NOTES = "Production-safe alert auto-clear allowlist and soak-cycle telemetry hardening";
