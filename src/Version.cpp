#include "Version.h"

// Bump this string and the release notes on each new release.
// Keep the value in sync with the Doxyfile PROJECT_NUMBER.

const char* FIRMWARE_VERSION = "v23-Diag-Soak";
const char* FIRMWARE_RELEASE_NOTES = "Unconditional Boron USB source override + compiled build-flag witness";
