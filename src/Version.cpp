#include "Version.h"

// Bump this string and the release notes on each new release.
// Keep the value in sync with the Doxyfile PROJECT_NUMBER.

const char* FIRMWARE_VERSION = "v25-Facades-BenchDiag";
const char* FIRMWARE_RELEASE_NOTES = "Step 5 persistence facades (MyPersistentData.h split by concern, no layout change) + bench-only lastConnection/OccupancyWebhook diagnostic logging";
