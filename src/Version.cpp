#include "Version.h"

// Bump this string and the release notes on each new release.
// Keep the value in sync with the Doxyfile PROJECT_NUMBER.

const char* FIRMWARE_VERSION = "6.00";
const char* FIRMWARE_RELEASE_NOTES = "Heap churn reduction - eliminate mergeConfiguration and String allocations in config path";
