#include "Version.h"

// Bump this string and the release notes on each new release.
// Keep the value in sync with the Doxyfile PROJECT_NUMBER.

const char* FIRMWARE_VERSION = "v22-Diag-Soak";
const char* FIRMWARE_RELEASE_NOTES = "Diagnostics publish mode promoted + serial wait off";
